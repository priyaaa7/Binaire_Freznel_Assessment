# Multi-user queueing system 

## Stack
React + Vite, Node + Express, express-fileupload, csv-parser, worker_threads



## Main

queue -> scheduler -> worker chunks -> partial sums -> final result -> status updates.

## Run
Backend:
cd server
npm install
npm run dev

Frontend:
cd client
npm install
npm run dev

